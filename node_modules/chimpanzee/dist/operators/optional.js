"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.optional = optional;

var _results = require("../results");

var _schemas = require("../schemas");

var _parse = require("../parse");

var _parse2 = _interopRequireDefault(_parse);

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*       */
function optional(schema, params = {}) {
  const meta = { type: "optional", schema, params };

  function fn(obj, key, parents, parentKeys) {
    return function (context) {
      const result = (0, _parse2.default)(schema)(obj, key, parents, parentKeys)(context);
      return !(result instanceof _results.Skip) ? result : new _results.Empty({ obj, key, parents, parentKeys }, meta);
    };
  }

  return new _schemas.FunctionSchema(fn, (0, _utils.getParams)(params), meta);
}
//# sourceMappingURL=optional.js.map