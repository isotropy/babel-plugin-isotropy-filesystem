"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.map = map;

var _results = require("../results");

var _schemas = require("../schemas");

var _parse = require("../parse");

var _parse2 = _interopRequireDefault(_parse);

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*       */
function map(schema, mapper, params) {
  const meta = { type: "map", schema, mapper, params };

  function fn(obj, key, parents, parentKeys) {
    return function (context) {
      const result = (0, _parse2.default)(schema)(obj, key, parents, parentKeys)(context);
      return result instanceof _results.Match ? new _results.Match(mapper(result.value), { obj, key, parents, parentKeys }, meta) : result;
    };
  }

  return new _schemas.FunctionSchema(fn, (0, _utils.getParams)(params), meta);
}
//# sourceMappingURL=map.js.map