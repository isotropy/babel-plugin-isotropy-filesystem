"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.regex = regex;

var _capture = require("./capture");

var _results = require("../results");

var _schemas = require("../schemas");

var _parse = require("../parse");

var _parse2 = _interopRequireDefault(_parse);

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function regex(regex, params) {
  const meta = { type: "regex", regex, params };

  function fn(obj, key, parents, parentKeys) {
    return function (context) {
      const result = (0, _parse2.default)((0, _capture.captureIf)(function (obj) {
        return typeof regex === "string" ? typeof obj === "string" && new RegExp(regex).test(obj) : typeof obj === "string" && regex.test(obj);
      }))(obj, key, parents, parentKeys)(context);
      return result instanceof _results.Skip ? new _results.Skip(`Did not match regex.`, { obj, key, parents, parentKeys }, meta) : result;
    };
  }
  return new _schemas.FunctionSchema(fn, (0, _utils.getParams)(params), { name: "regex" });
} /*       */
//# sourceMappingURL=regex.js.map